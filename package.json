{
    "name": "world-app-workspace",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm --filter world-app dev",
        "tauri": "tauri"
    },
    "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
    },
    "packageManager": "pnpm@8.6.1",
    "volta": {
        "node": "22.12.0"
    },
    "lint-staged": {
        "app/**/*.{js,jsx,ts,tsx}": [
            "eslint --fix",
            "prettier --write"
        ],
        "client/**/*.{js,jsx,ts,tsx}": [
            "eslint --fix",
            "prettier --write"
        ]
    }
}
