server {
  listen 80;

  location = /cc-info {
    alias /cc-info;
  }

  location / {
    alias /usr/share/nginx/html/;
    index index.html index.htm;
    try_files $uri $uri/ /index.html =404;
  }
}
